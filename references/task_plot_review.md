# Task Plot Review

## Outcome

Accepted after one targeted regeneration.

## Visual QA

- Layout: four rows are evenly spaced, legible, and free of overlap.
- Exact strings: `HOUSE`, `THIMBLE`, and `CEMORE` are spelled correctly; the pseudoword was not normalized into a real word.
- Response mapping: F is shown only for words and J only for the pseudoword; the wrong-key row explicitly identifies a J response to HOUSE.
- Feedback: correct rows proceed directly to ITI; only the wrong-key row shows red ERROR for 750 ms.
- Stimulus states: fixation screens contain one plus, string screens contain one string, and ITI screens are blank.
- Timing: 500 ms fixation, up to 2000 ms response, 750 ms error feedback, and 150 ms ITI are accurate.
- Scale: strings and fixation fit comfortably inside consistent black participant screens.

## Revision Note

The first generation labeled the error branch only as `Wrong key`. The accepted regeneration added the missing `J response` label without changing any stimulus, timing, or correct-response mapping.
